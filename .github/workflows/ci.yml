# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['**', '!update/**', '!pr/**']
  push:
    branches: ['**', '!update/**', '!pr/**']

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.11]
        java: [temurin@8]
        cbor_enabled: [true, false]
        service_port: [4567, 4568]
        project: [root-jvm-213, root-js-213]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Download Java (temurin@8)
        id: download-java-temurin-8
        if: matrix.java == 'temurin@8'
        uses: typelevel/download-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Setup Java (temurin@8)
        if: matrix.java == 'temurin@8'
        uses: actions/setup-java@v3
        with:
          distribution: jdkfile
          java-version: 8
          jdkFile: ${{ steps.download-java-temurin-8.outputs.jdkFile }}

      - name: Cache sbt
        uses: actions/cache@v3
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Check that workflows are up to date
        run: sbt githubWorkflowCheck

      - name: Link JS
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest' && startsWith(matrix.project, 'root-js')
        run: sbt '++ ${{ matrix.scala }}' Test/fastLinkJS

      - name: Unit Tests
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest'
        run: sbt '++ ${{ matrix.scala }}' unit-tests/test

      - name: Docker Compose Up
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest'
        uses: nick-fields/retry@v2
        with:
          timeout_minutes: 15
          retry_on: error
          command: sbt 'project ${{ matrix.project }}' dockerComposeUp
          on_retry_command: sbt 'project ${{ matrix.project }}' dockerComposeDown
          max_attempts: 3

      - name: Integration Tests
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest'
        run: sbt '++ ${{ matrix.scala }}' integration-tests/test

      - name: Print docker logs and container listing
        if: ${{ failure() }}
        run: sbt '++ ${{ matrix.scala }}' dockerComposePs dockerComposeLogs

      - name: Remove docker containers
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest'
        run: sbt '++ ${{ matrix.scala }}' dockerComposeDown

      - name: Check scalafix lints
        if: matrix.java == 'temurin@8' && matrix.os == 'ubuntu-latest'
        run: sbt '++ ${{ matrix.scala }}' fixCheck
